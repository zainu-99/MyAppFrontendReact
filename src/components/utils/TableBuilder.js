function TableBuilder(listOfdata) {
    const items = listOfdata.length
    if(items >0){
        let counter =0
        Object.keys(items[0]).forEach((e)=> {
            
        })
        listOfdata.forEach((e)=>{
            
        })

    }
}

export default TableBuilder
